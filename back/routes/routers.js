const loginRouter = require('./login.router');
const userRouter = require('./user.router');
const transactionsRouter = require('./transations.router');

module.exports = {
  loginRouter,
  userRouter,
  transactionsRouter
}
